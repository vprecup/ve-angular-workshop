import { ToDayOfWeekPipe } from './to-day-of-week.pipe';

describe('ToDayOfWeekPipe', () => {
  it('create an instance', () => {
    const pipe = new ToDayOfWeekPipe();
    expect(pipe).toBeTruthy();
  });
});
